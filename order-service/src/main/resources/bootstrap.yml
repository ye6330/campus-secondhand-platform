spring:
  application:
    name: campus-order-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:192.168.209.128:8848}
      config:
        server-addr: ${NACOS_ADDR:192.168.209.128:8848}
        file-extension: yml
