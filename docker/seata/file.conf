# Seata Server 配置（file 模式存储全局事务，不依赖数据库存储）
store {
  mode = "file"

  file {
    dir = "sessionStore"
  }
}

service {
  vgroupMapping.campus_tx_group = "default"
  grouplist.default = "seata-server:8091"
}
